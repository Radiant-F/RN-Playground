import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {BottomNavigator} from '../../routes/Navigator';

export default function Settings() {
  return (
    <View style={{flex: 1}}>
      <Text>Settings</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
